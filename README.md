# docker-php7-phalcon-devtools
The core, production ready, PHP7 Phalcon container with additional development tools installed

```
docker build -t phalcon-dev .
docker run --name phalcon-dev -it phalcon-dev
```
